syntax = "proto3";
package api;

import "util.proto";

service UserService {
	rpc Signup (SignupRequest) returns (Empty);
}

message SignupRequest {
	string firebaseUid = 1;
	string username = 2;
	string email = 3;
}
