syntax = "proto3";

service UserService {
	rpc Signup (SignupRequest) returns (SignupResponse);
}

message SignupRequest {
	string username = 1;
	string email = 2;
}

message SignupResponse {
	uint64 id = 1;
}
