syntax = "proto3";
package api;

import "google/protobuf/empty.proto";

service PrizeService {
	rpc GetCurrentPrize (google.protobuf.Empty) returns (GetCurrentPrizeResponse);
}

message GetCurrentPrizeResponse {
	uint64 id = 1;
	bool isWon = 2;
	repeated string photos = 3;
}
